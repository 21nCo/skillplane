# Shared Linear invocation

At invocation, accept the parent-prepared issue summary, body and authorized destination. Resolve the destination, check for duplicates, apply only the authorized issue creation or association operation, and read back the resulting record. Return the issue ID, URL, destination and association receipt. Report partial writes accurately. This module grants no additional mutation authority.
