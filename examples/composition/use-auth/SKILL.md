# Authentication reference module

Use the repository authentication provider. Enforce authenticated identity and tenant authorization on each protected operation. Follow the parent workflow authorization boundary.
