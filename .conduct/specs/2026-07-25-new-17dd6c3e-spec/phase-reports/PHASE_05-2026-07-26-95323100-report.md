# PHASE_05 completion report

## Metadata

| Field | Value |
|---|---|
| Timestamp | `2026-07-26T07:56:53Z` |
| Agent | `unknown-agent` |
| Model | `GPT-5` |
| Launcher | `Codex Desktop` |
| Workspace | repository root |
| Environment | `Darwin arm64`, shell `zsh` |
| Git | branch `main`; no commit yet; repository working tree contains the implementation |

## Phase

`PHASE_05` — Skill bundles, R2, versions, and search

## Status

**PASS**

Secure deterministic skill bundles, immutable private R2 persistence,
monotonic revisions, atomic semantic publication, recoverable lifecycle, and
authorization-safe Postgres full-text search are implemented through the
storage, domain, database, Hono, and test boundaries.

## Requirements delivered

`DATA-004`, `TEN-003`, `SKL-001`, `SKL-002`, `SKL-003`, `SKL-004`,
`SKL-005`, `SKL-009`, `SKL-010`, `OPS-005`, `OPS-006`, `QA-001`, `QA-003`,
and `QA-004`.

### DATA-004 — PASS

- Canonical bundles use private keys of the required
  `workspaces/{workspaceId}/skills/{skillId}/bundles/sha256/{digest}.zip`
  shape.
- Conditional create plus full-byte/digest verification prevents a content
  address from being overwritten with different bytes.
- Integration evidence proves same-request replay retains one object and R2
  write failure creates no version row.
- Tests: `packages/storage/src/r2.test.ts`,
  `packages/api/tests/integration/skill-storage.integration.test.ts`.

### TEN-003 — PASS

- Private/workspace reads require an authorized workspace principal.
- Anonymous reads return only public, published, non-archived skills and
  versions.
- Candidate versions and private skills return non-leaking `404` responses to
  anonymous callers. Context terms never enter the public search document.
- Tests: `packages/api/tests/security/tenant-search.security.test.ts`.

### SKL-001 — PASS

- Editor-or-higher creation validates/canonicalizes the bundle, writes R2,
  creates the skill and revision 1, publishes `1.0.0`, sets the release pointer,
  and appends audit/idempotency state in one Postgres transaction.
- Duplicate slug and forced R2 failures leave no visible partial skill and no
  retained orphan.
- Tests: `packages/api/tests/integration/skill-storage.integration.test.ts`.

### SKL-002 — PASS

- Root `SKILL.md` and `skill.json` are required; only `assets/`,
  `references/`, and `scripts/` optional roots are accepted.
- `skill.json` is strict format version 1 and declares every non-self file
  digest. The canonical database manifest includes all files, including
  canonical `skill.json`.
- Missing roots, undeclared files, mismatched digests, and unsupported roots
  return typed validation errors before storage.
- Tests: `packages/storage/src/validate.test.ts`,
  `packages/storage/tests/security/bundle.security.test.ts`.

### SKL-003 — PASS

- Equivalent inventories with different ZIP order/timestamps produce identical
  canonical bytes and SHA-256.
- Central-directory inspection rejects traversal, absolute/backslash paths,
  invalid UTF-8, case-fold collisions, encryption, ZIP64/multidisk archives,
  unsupported compression, links/special modes, inconsistent headers, and all
  configured expansion limits before persistence.
- Property test: 75 generated inventories, seed `20260725`.
- Tests: `packages/storage/src/canonicalize.test.ts`,
  `packages/storage/tests/security/bundle.security.test.ts`.

### SKL-004 — PASS

- Candidate revision numbers are allocated under a per-skill row lock in a
  committed reservation transaction and never reused.
- Forced R2 failure reserved revision 2; successful candidates used revisions
  3 and 4.
- Published-row/file triggers reject direct mutation. Historical keys and rows
  remain intact.
- Tests: `packages/api/tests/integration/publication-concurrency.integration.test.ts`,
  `packages/db/tests/integration/schema.integration.test.ts`.

### SKL-005 — PASS

- Candidate semantic versions remain null until publication.
- Approval locks the skill/candidate, checks the immutable current base,
  computes the then-current semantic bump, publishes, and advances the pointer
  atomically.
- Concurrent approval produced one `1.0.1` winner and one typed `409`; the
  loser remained pending. Rejection later preserved its original summary and
  recorded the reason in immutable audit.
- Test: `packages/api/tests/integration/publication-concurrency.integration.test.ts`.

### SKL-009 — PASS

- Archive removes the skill from default lists/search without changing the
  release pointer or version history.
- Authorized exact historical version/file retrieval remains available while
  archived. Anonymous/current discovery does not.
- Restore returns the unchanged pointer and visibility to default listings.
- Test: `packages/api/tests/integration/skill-storage.integration.test.ts`.

### SKL-010 — PASS

- Generated public/workspace `tsvector` columns index name, description, tags,
  published instructions, and only authorized context metadata.
- A materialized authorization CTE executes tenant/visibility filters before
  ranking.
- Stable integer scores plus stable ID ties support deterministic signed,
  expiring, filter-bound cursors.
- Cross-tenant results/scores, candidates, public context terms, cursor filter
  changes, signature tampering, and non-canonical encodings are tested.
- `pnpm db:verify` reports `skills_public_search_idx` and
  `skills_workspace_search_idx`.

### OPS-005 — PASS

- Required tenant/revision/search indexes have verified query plans.
- Worker bundle-body caching is keyed only by immutable SHA-256 after route
  authorization; every cache hit first confirms R2 object availability and
  re-hashes bytes.
- Private/mutable HTTP responses use `private, no-store`; active files use CSP
  sandboxing and attachment disposition.
- Tests: `packages/storage/src/r2.test.ts`,
  `packages/storage/src/downloads.test.ts`; database verifier.

### OPS-006 — PASS

- Migration 0008 applies forward and all eight migration hashes verify.
- Orphan cleanup completes the full R2 and database inventories before
  deletion, uses an age cutoff, preserves referenced objects, and fails closed
  on uncertainty.
- The local rehearsal deletes only an explicit orphan and verifies the
  referenced bundle remains retrievable by digest.
- Runbook: `docs/operations/database-recovery.md`.

### QA-001 — PASS

- Exact deterministic unit, integration, concurrency, security, and schema
  tests cover positive, negative, boundary, failure, authorization, caching,
  and recovery behavior.
- Final exact phase test total: 15 storage unit, 11 domain unit, 6 storage
  integration, 4 bundle security, 5 search security, and 1 publication
  concurrency test.

### QA-003 — PASS

- Untrusted ZIP bytes are inspected before decompression or persistence.
- Stored scripts are never executed.
- R2 failures and digest mismatches fail closed without alternate content.
- R2 keys are not exposed; private responses are not shared-cacheable; active
  file types are sandboxed and downloaded.

### QA-004 — PASS

- Canonical digests, limits, R2 inventory, revision gaps, publication results,
  search isolation, defects, design choices, test commands, and recovery steps
  are recorded in the engineering log, observation, ledger, CSV logs, and this
  report.

## Deliverables summary

### Added

- `packages/storage/package.json`
- `packages/storage/tsconfig.json`
- `packages/storage/src/manifest.ts`
- `packages/storage/src/paths.ts`
- `packages/storage/src/validate.ts`
- `packages/storage/src/canonicalize.ts`
- `packages/storage/src/r2.ts`
- `packages/storage/src/downloads.ts`
- `packages/storage/src/index.ts`
- `packages/storage/src/canonicalize.test.ts`
- `packages/storage/src/validate.test.ts`
- `packages/storage/src/r2.test.ts`
- `packages/storage/src/downloads.test.ts`
- `packages/storage/tests/support/bundle-fixture.ts`
- `packages/storage/tests/fixtures/canonical-digests.json`
- `packages/storage/tests/security/bundle.security.test.ts`
- `packages/domain/src/idempotency.ts`
- `packages/domain/src/transactions.ts`
- `packages/domain/src/skills.ts`
- `packages/domain/src/skill-versions.ts`
- `packages/domain/src/publication.ts`
- `packages/domain/src/search.ts`
- `packages/api/src/routes/skills.ts`
- `packages/api/src/routes/skill-versions.ts`
- `packages/api/src/routes/search.ts`
- `packages/api/tests/integration/skill-storage.integration.test.ts`
- `packages/api/tests/integration/publication-concurrency.integration.test.ts`
- `packages/api/tests/security/tenant-search.security.test.ts`
- `packages/db/migrations/0008_skill_bundles_search.sql`
- `packages/testing/src/skill-bundles.ts`

### Modified

- `pnpm-workspace.yaml`, `pnpm-lock.yaml`
- `packages/config/src/index.ts`, `packages/config/src/index.test.ts`
- `packages/db/src/schema/domain.ts`, `packages/db/src/verify.ts`
- `packages/db/tests/integration/schema.integration.test.ts`
- `packages/db/src/migrate.test.ts`
- `packages/domain/package.json`, `packages/domain/src/index.ts`
- `packages/domain/src/errors.ts`, `packages/domain/src/authorization.ts`
- `packages/domain/src/authorization.test.ts`
- `packages/api/package.json`, `packages/api/src/app.ts`
- `packages/api/src/context.ts`, `packages/api/src/services.ts`
- `packages/api/src/middleware/authorization.ts`
- `packages/api/src/routes/shared.ts`
- `packages/testing/package.json`, `packages/testing/src/index.ts`
- `packages/testing/src/runtime.ts`, `packages/testing/src/fixtures.ts`
- `scripts/run-integration-tests.mjs`, `scripts/run-security-tests.mjs`
- `docs/operations/database-recovery.md`

No production source file was deleted. No Superfunctions file was changed.

## Verification summary

| Command | Result |
|---|---|
| `pnpm test:unit --filter @skillplane/storage --filter @skillplane/domain` | PASS — storage 15/15, domain 11/11 |
| `pnpm test:integration --filter skill-storage` | PASS — 6/6 |
| `pnpm test:security --filter bundle` | PASS — 4/4 |
| `pnpm test:security --filter tenant-search` | PASS — 5/5 |
| `pnpm test:integration --filter publication-concurrency` | PASS — 1/1 |
| `pnpm db:verify` | PASS — 21 tables, 8 migrations, required triggers/constraints/GIN plans |

Supplemental final-source runs passed:

- API integration: 19/19.
- API security: 11/11.
- Database integration: 4/4.
- Storage, domain, configuration, testing, API, and database typechecks.

## Canonical fixture and stop-condition evidence

| Fixture | Digest | Bytes |
|---|---|---:|
| Minimal | `sha256:89a1ce5aedcab433c01bee721e93fd9a989a0bc4aa8ab4f54c4d4cb21bae376f` | 465 |
| Portable | `sha256:c80839837c5bad80842caf1035ff26b82c9a0836c5416cf96ad797c64c24a972` | 900 |

- R2 cleanup inventory: scanned 2, deleted 1 orphan, preserved 1 referenced,
  final 1.
- Publication inventory: three immutable objects; revisions `1`, `3`, `4`;
  one concurrent `1.0.1` winner and one conflict.
- Search: workspace pre-ranking isolation, anonymous public-only results,
  candidate/context exclusion, deterministic paging, and cursor tamper
  rejection all passed.

## Notes

1. `skill.json.files` excludes `skill.json` itself because a file cannot contain
   its own SHA-256. The canonical database manifest includes `skill.json` and
   its actual digest, so the persisted inventory is complete.
2. Candidate revisions reserve a monotonic revision in a committed transaction;
   failed external storage attempts intentionally leave gaps.
3. Semantic versions are assigned only at publication.
4. Public search intentionally excludes context metadata; workspace search
   includes non-archived context name/description.
5. Bundle caching is internal and digest-keyed after authorization. HTTP
   authorization and mutable pointers remain `private, no-store`.
6. Feature UI, contexts, and agent amendment policies remain outside PHASE_05
   exactly as specified.
7. No screenshot was required because this phase explicitly excludes feature
   UI; automated storage/search observations are recorded instead.

## Ready for next phase?

**Yes.** Every PHASE_05 deliverable and acceptance criterion has objective
passing evidence. The stop-condition inventory is recorded above.

## Blockers

None.
